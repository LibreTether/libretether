# Keep the Docker build context small — the relay image only needs the Cargo
# workspace source, never the frontend's deps, build artifacts, or git history.
**/node_modules
**/target
**/dist
**/*.swp
.git
.github
